# CALCULATOR
A small, ready-to-use calculator web app you can drop into a repository. The project is split into separate files (HTML, CSS, JS) so it looks and feels like a normal hand-crafted project.
